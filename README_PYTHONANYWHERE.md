# 🚀 Развертывание Telegram бота на PythonAnywhere

Полное руководство по развертыванию Groq Telegram бота на PythonAnywhere платформе.

## 📋 Требования

- **PythonAnywhere аккаунт** с платным планом (Developer или выше)
- **Telegram Bot Token** от [@BotFather](https://t.me/BotFather)
- **Groq API Key** от [console.groq.com](https://console.groq.com/)
- **GitHub репозиторий** с исходным кодом

## 💰 Цены PythonAnywhere (2026)

| План | Цена | CPU в день | Диск | Always-On |
|------|------|------------|------|-----------|
| Free | $0 | 100 секунд | 512 MiB | ❌ Нет |
| Developer | $10/мес | 5,000 секунд | 5 GB | ✅ Есть |
| Hacker | $30/мес | 20,000 секунд | 20 GB | ✅ Есть |

**Важно:** Для 24/7 работы Telegram бота нужен платный план с Always-On задачами.

## 🛠️ Способ 1: Автоматическое развертывание (рекомендуется)

### Шаг 1: Подготовка локально
```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/groq_tgbot.git
cd groq_tgbot

# Проверьте файлы
ls -la
# Должны быть: bot.py, requirements.txt, .env.example, pythonanywhere.sh
```

### Шаг 2: Запуск развертывания на PythonAnywhere

1. **Войдите в PythonAnywhere Dashboard**
2. **Откройте Bash консоль** (Web → Consoles → Bash)
3. **Скачайте скрипт развертывания:**

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/groq_tgbot.git
cd groq_tgbot

# Сделайте скрипт исполняемым
chmod +x pythonanywhere.sh

# Запустите развертывание
./pythonanywhere.sh
```

### Шаг 3: Настройка токенов
Скрипт автоматически создаст `.env` файл. Отредактируйте его:

```bash
nano /home/yourusername/groq-tgbot/.env
```

Добавьте ваши токены:
```bash
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
GROQ_API_KEY=gsk_abc123def456ghi789jkl012mno345pqr
PYTHONANYWHERE=true
```

### Шаг 4: Создание Always-On задачи

1. **В PythonAnywhere Dashboard** перейдите в **Tasks**
2. **Прокрутите вниз** до "Always-on tasks"
3. **Нажмите "Add an always-on task"**

**Настройки задачи:**
- **Command:** `/home/yourusername/groq-tgbot/run_bot.sh`
- **Description:** `Groq Telegram Bot`
- **Minute:** `*/1` (проверка каждую минуту)

4. **Нажмите "Create"**

## 🛠️ Способ 2: Ручное развертывание

### Шаг 1: Подготовка окружения
```bash
# Создайте директорию проекта
mkdir -p ~/groq-tgbot
cd ~/groq-tgbot

# Клонируйте репозиторий
git clone https://github.com/yourusername/groq_tgbot.git .

# Создайте виртуальное окружение
python3.10 -m venv venv
source venv/bin/activate

# Обновите pip и установите зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 2: Настройка переменных окружения
```bash
# Создайте .env файл
cp .env.example .env
nano .env
```

### Шаг 3: Тестовый запуск
```bash
# Тестовый запуск (Ctrl+C для остановки)
export PYTHONANYWHERE=true
python3.10 -u bot.py
```

### Шаг 4: Создание Always-On задачи
Как в Способе 1, шаг 4, но с командой:
```bash
source /home/yourusername/groq-tgbot/venv/bin/activate && cd /home/yourusername/groq-tgbot && export PYTHONANYWHERE=true && python3.10 -u bot.py
```

## 🔍 Мониторинг и отладка

### Проверка статуса
```bash
# Проверьте логи Always-On задачи в PythonAnywhere Dashboard
# Статус должен быть "Running"

# Проверьте активность бота
grep -i "running\|error" /home/yourusername/groq-tgbot/run_bot.log
```

### Перезапуск бота
```bash
# В PythonAnywhere Dashboard:
# 1. Остановите Always-On задачу
# 2. Подождите 1 минуту
# 3. Запустите задачу снова
```

### Просмотр логов
```bash
# Просмотр последних 100 строк лога
tail -100 /home/yourusername/groq-tgbot/run_bot.log

# Отслеживание логов в реальном времени
tail -f /home/yourusername/groq-tgbot/run_bot.log
```

## ⚠️ Частые проблемы и решения

### Проблема: Bot stops after a few hours
**Причина:** Free план или неверная настройка Always-On
**Решение:** Убедитесь что у вас платный план и Always-On задача запущена

### Проблема: "ModuleNotFoundError"
**Причина:** Неправильная активация виртуального окружения
**Решение:** Проверьте команду запуска Always-On задачи
```bash
# Правильная команда:
cd /home/username/groq-tgbot && source venv/bin/activate && python3.10 -u bot.py
```

### Проблема: "Token not found"
**Причина:** Не настроен .env файл
**Решение:** Проверьте содержимое .env файла
```bash
cat /home/username/groq-tgbot/.env
```

### Проблема: Groq API errors
**Причина:** Проблемы с API ключом или лимиты
**Решение:** 
1. Проверьте валидность Groq API ключа
2. Проверьте баланс в Groq консоли
3. Добавьте обработку ошибок в код

### Проблема: Bot doesn't respond
**Причина:** Бот не запущен или ошибки в коде
**Решение:**
1. Проверьте статус Always-On задачи
2. Посмотрите логи ошибок
3. Протестируйте запуск вручную в консоли

## 🔧 Оптимизация для PythonAnywhere

### Экономия CPU секунд
- **Ограничение истории сообщений** (10 сообщений в коде)
- **Таймауты запросов** (30 секунд для Groq API)
- **Оптимизированный polling** с правильными интервалами

### Мониторинг ресурсов
```bash
# Проверьте использование CPU в PythonAnywhere Dashboard
# Норма: < 1000 CPU секунд в день для простого бота
```

### Автоматическое обновление
```bash
# Создайте scheduled задачу для обновления:
# Command: cd /home/username/groq-tgbot && git pull && source venv/bin/activate && pip install -r requirements.txt
# Schedule: 0 3 * * * (каждый день в 3:00)
```

## 📊 Структура проекта

```
groq-tgbot/
├── bot.py                    # Основной код бота
├── requirements.txt           # Зависимости Python
├── .env.example             # Пример конфигурации
├── .env                     # Ваши токены (создается при развертывании)
├── pythonanywhere.sh        # Скрипт автоматического развертывания
├── venv/                   # Виртуальное окружение
├── run_bot.sh              # Скрипт для Always-On задачи
└── README_PYTHONANYWHERE.md  # Этот файл
```

## 🎯 Готовность к продакшену

После выполнения всех шагов ваш бот будет:
- ✅ **Работать 24/7** на PythonAnywhere
- ✅ **Автоматически перезапускаться** при сбоях
- ✅ **Использовать бесплатный Groq API**
- ✅ **Иметь логирование** для отладки
- ✅ **Быть оптимизированным** для экономии ресурсов

## 📞 Поддержка

- **PythonAnywhere documentation:** https://help.pythonanywhere.com/
- **python-telegram-bot documentation:** https://python-telegram-bot.org/
- **Groq API documentation:** https://console.groq.com/docs

---

**🎉 Поздравляю! Ваш Telegram бот готов к работе на PythonAnywhere!**